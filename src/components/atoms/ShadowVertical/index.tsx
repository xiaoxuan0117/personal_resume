import * as React from "react";
import { ShadowVerticalLine } from "./index.style";

export interface IShadowVerticalProps {}

export default function ShadowVertical(props: IShadowVerticalProps) {
  return <ShadowVerticalLine />;
}
